name: restrict-image-registries-test
policies:
  - policy.yaml
resources:
  - resources.yaml
results:
  - policy: restrict-image-registries
    rule: restrict-image-registries
    resource: good-pod
    kind: Pod
    result: pass
  - policy: restrict-image-registries
    rule: restrict-image-registries
    resource: bad-pod-dockerhub
    kind: Pod
    result: fail
  - policy: restrict-image-registries
    rule: restrict-image-registries
    resource: bad-pod-init-container-wrong-registry
    kind: Pod
    result: fail
